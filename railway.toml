[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs_20"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"

# Одноразове очищення БД при цьому деплої — видаліть після успішного запуску
[variables]
CLEAR_DB = "1"
